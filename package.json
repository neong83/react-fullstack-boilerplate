{
  "name": "fullstack_toy_project",
  "version": "1.0.0",
  "description": "",
  "author": "Hou Ben Ng",
  "scripts": {
    "start": "npm run start:web & npm run start:server",
    "start:web": "cd frontend && npm start && cd ..",
    "start:server": "cd backend && npm start && cd ..",

    "build": "npm run build:web & npm run build:server",
    "build:web": "cd frontend && npm run build && cd ..",
    "build:server": "cd backend && npm run build && cd ..",

    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.4.1"
  }
}
