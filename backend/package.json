{
  "main": "dist/index.js",
  "scripts": {
    "prestart": "npm run build",
    "start": "node .",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.0",
    "typescript": "^4.4.4"
  }
}
